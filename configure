#!/usr/bin/env bash

rm --recursive --force out_debug out_release

CXX="clang++" CXXFLAGS="-fsanitize=address,undefined" \
  meson setup "out_debug" --buildtype debug

CXX="clang++" CXXFLAGS="-march=native" \
  meson setup "out_release" --buildtype release
